query Query${{ name }}{% if args and args.length > 0 %}({% for arg in args %}${{ arg.name }}: {{ arg.type }}{% if forloop.last == false %} {% endif %}{% endfor %} $skipNullArguments: Boolean){% endif %} {
  {{ name }}{% if args and args.length > 0 %}({% for arg in args %}{{ arg.name }}: ${{ arg.name }}{% if forloop.last == false %} {% endif %}{% endfor %}) @skipNullArguments(if: $skipNullArguments){% endif %}
    {%- if isConnection %}
    totalCount
    edges {
        node
          {%- if fields %} {
          {%- for field in fields %}
          {{ field.name }}
          {%- endfor %}
        }
          {%- endif %}
    }
    {%- else -%}
    {%- if fields %} {
    {%- for field in fields %}
    {{ field.name }}
    {%- endfor %}
  }
    {%- endif %}
    {%- endif %}
}