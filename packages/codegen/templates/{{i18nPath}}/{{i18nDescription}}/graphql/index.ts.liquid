import type { NamespaceGraphqlTranslation } from '../../i18n-types';

const {{ lang }}: NamespaceGraphqlTranslation = {
{%- if objects %}
	objects: {
{%- for object in objects %}
		{{ object.name }}: {
			name: '{{ object.description || object.name }}',
			fields: {
{%- for field in object.fields %}
				{{ field.name }}: {
					name: '{{ field.description || field.name }}',
				}{%- if forloop.last == false -%},{%- endif %}
{%- endfor %}
			}
		}{%- if forloop.last == false -%},{%- endif %}
{%- endfor %}
	},
{%- endif %}
{%- if enums %}
	enums: {
{%- for enum in enums %}
		{{ enum.name }}: {
			name: '{{ enum.description || enum.name }}',
			values: {
{%- for value in enum.values %}
				{{ value.name }}: {
					name: '{{ value.description || value.name }}',
				}{%- if forloop.last == false -%},{%- endif %}
{%- endfor %}
			}
		}{%- if forloop.last == false -%},{%- endif %}
{%- endfor %}
	}
{%- endif %}
}

export default {{ lang }};