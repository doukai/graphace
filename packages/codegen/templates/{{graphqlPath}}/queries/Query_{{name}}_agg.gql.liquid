query Query_{{ name }}
  {%- if variables and variables.length > 0 -%}
  ({% for variable in variables %}{% if variable.alias %}${{ variable.alias }}{% else %}${{ variable.name }}{% endif %}: {{ variable.type }}{% if variable.default %} = {{ variable.default }}{% endif %}{% if forloop.last == false %}, {% endif %}{% endfor %})
  {%- endif %} {
  {{ name }}
    {%- if args and args.length > 0 %}({% for arg in args %}{{ arg.name }}: {% if arg.alias %}${{ arg.alias }}{% else %}${{ arg.name }}{% endif %}{% if forloop.last == false %} {% endif %}{% endfor %}){% endif %}
    {%- if isConnection %} {
    totalCount
    edges {
      node {
        {%- if fields %}
        {%- for field in fields %}
        {{ field.name }} @include(if: $include_{{ field.name }})
        {%- endfor %}
        {%- endif %}
        {%- if namedFields or fileFields %}
        {%- for field in namedFields %}
        {%- if field.select %}
        {{ field.name }} @include(if: $include_{{ field.name }}) {
          {{ field.fieldTypeIdName }}
          name
          description
        }
        {%- else %}
        {{ field.name }}{% if field.isListType %}(first: 3){% endif %} @include(if: $include_{{ field.name }}) {
          name
          description
        }
        {%- endif %}
        {%- endfor %}
        {%- for field in fileFields %}
        {{ field.name }} @include(if: $include_{{ field.name }}) {
          id
          name
        }
        {%- endfor %}
        {%- endif %}
      }
    }
  }
    {%- else %}
    {%- if fields or namedFields or fileFields %} {
    {%- if fields %}
    {%- for field in fields %}
    {{ field.name }} @include(if: $include_{{ field.name }})
    {%- endfor %}
    {%- endif %}
    {%- if namedFields %}
    {%- for field in namedFields %}
    {%- if field.select %}
    {{ field.name }} @include(if: $include_{{ field.name }}) {
      {{ field.fieldTypeIdName }}
      name
      description
    }
    {%- else %}
    {{ field.name }}{% if field.isListType %}(first: 3){% endif %} @include(if: $include_{{ field.name }}) {
      name
      description
    }
    {%- endif %}
    {%- endfor %}
    {%- for field in fileFields %}
    {{ field.name }} @include(if: $include_{{ field.name }}) {
      id
      name
    }
    {%- endfor %}
    {%- endif %}
  }
    {%- endif %}
    {%- endif %}
}