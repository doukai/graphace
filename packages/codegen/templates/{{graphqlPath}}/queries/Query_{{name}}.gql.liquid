query Query_{{ name }}
  {%- if args and args.length > 0 -%}
  ({% for arg in args %}{% if arg.alias %}${{ arg.alias }}{% else %}${{ arg.name }}{% endif %}: {{ arg.type }}{% if forloop.last == false %}, {% endif %}{% endfor %}, $skipNullArguments: Boolean = false)
  {%- endif %} {
  {{ name }}
    {%- if args and args.length > 0 %}({% for arg in args %}{{ arg.name }}: {% if arg.alias %}${{ arg.alias }}{% else %}${{ arg.name }}{% endif %}{% if forloop.last == false %} {% endif %}{% endfor %}){%- if args and args.length > 0 %} @skipNullArguments(if: $skipNullArguments){% endif %}{% endif %}
    {%- if isConnection %} {
    totalCount
    edges {
      node
        {%- if fields %} {
        {%- for field in fields %}
        {{ field.name }}
        {%- endfor %}
      }
        {%- endif %}
    }
  }
    {%- else %}
    {%- if fields %} {
    {%- for field in fields %}
    {{ field.name }}
    {%- endfor %}
  }
    {%- endif %}
    {%- endif %}
}