{
	"name": "@graphace/graphql",
	"version": "0.0.1",
	"description": "graphace graphql module",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"files": [
		"dist"
	],
	"scripts": {
		"clean": "rimraf dist",
		"build": "tsc",
		"watch:build": "tsc --watch",
		"publish:local": "pnpm publish --no-git-checks --registry http://localhost:4873/",
		"lint": "prettier --check --ignore-path=../../.prettierignore . && eslint \".\"",
		"format": "prettier --write --ignore-path=../../.prettierignore ."
	},
	"devDependencies": {
		"eslint-config-custom": "workspace:*"
	},
	"dependencies": {
		"typescript": "^4.9.5",
		"@graphace/commons": "workspace:*"
	},
	"peerDependencies": {}
}
