mutation Mutation_role_userRole($role_id: ID, $role_userRole: [UserRoleInput], $first: Int, $from: UserExpression, $groupBy: [String!], $id: StringExpression, $last: Int, $offset: Int, $orderBy: UserRoleOrderBy, $roleId: IntExpression, $to: RoleExpression, $totalCount: IntExpression, $userId: IntExpression, $skipNullArguments: Boolean = true, $update: Boolean = false, $mergeToList: [String]) {
  role(id: $role_id userRole: $role_userRole) @skipNullArguments(if: $skipNullArguments) @update(if: $update) @mergeToList(arguments: $mergeToList) {
    createGroupId
    createTime
    createUserId
    description
    id
    isDeprecated
    name
    realmId
    updateTime
    updateUserId
    version
    userRoleConnection(first: $first from: $from groupBy: $groupBy id: $id last: $last offset: $offset orderBy: $orderBy roleId: $roleId to: $to totalCount: $totalCount userId: $userId) @skipNullArguments(if: $skipNullArguments) @update(if: $update) @mergeToList(arguments: $mergeToList) {
      totalCount
      edges {
        node {
          createGroupId
          createTime
          createUserId
          id
          isDeprecated
          realmId
          roleId
          updateTime
          updateUserId
          userId
          version
        }
      }
    }
  }
}