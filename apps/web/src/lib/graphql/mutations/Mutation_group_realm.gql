mutation Mutation_group_realm($group_id: String, $group_realm: RealmInput, $id: StringExpression, $name: StringExpression, $groupBy: [String!], $mergeToList: [String]) {
  group(where: {id: {val: $group_id}}  realm: $group_realm) @mergeToList(arguments: $mergeToList) {
    id
    name
    path
    deep
    isDeprecated
    version
    realmId
    createUserId
    createTime
    updateUserId
    updateTime
    createGroupId
    syncGroupPolicy
    realm(id: $id name: $name groupBy: $groupBy) {
      id
      name
      isDeprecated
      version
      realmId
      createUserId
      createTime
      updateUserId
      updateTime
      createGroupId
    }
  }
}