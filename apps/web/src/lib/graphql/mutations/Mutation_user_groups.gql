mutation Mutation_user_groups($user_id: ID, $user_groups: [GroupInput], $first: Int, $groupBy: [String!], $id: StringExpression, $last: Int, $name: StringExpression, $offset: Int, $orderBy: GroupOrderBy, $parent: GroupExpression, $realm: RealmExpression, $roles: RoleExpression, $subGroups: GroupExpression, $totalCount: IntExpression, $users: UserExpression, $skipNullArguments: Boolean = true, $update: Boolean = false, $mergeToList: [String]) {
  user(id: $user_id groups: $user_groups) @skipNullArguments(if: $skipNullArguments) @update(if: $update) @mergeToList(arguments: $mergeToList) {
    createGroupId
    createTime
    createUserId
    disable
    email
    id
    isDeprecated
    lastName
    login
    name
    password
    realmId
    updateTime
    updateUserId
    version
    groupsConnection(first: $first groupBy: $groupBy id: $id last: $last name: $name offset: $offset orderBy: $orderBy parent: $parent realm: $realm roles: $roles subGroups: $subGroups totalCount: $totalCount users: $users) @skipNullArguments(if: $skipNullArguments) @update(if: $update) @mergeToList(arguments: $mergeToList) {
      totalCount
      edges {
        node {
          createGroupId
          createTime
          createUserId
          id
          isDeprecated
          name
          parentId
          realmId
          updateTime
          updateUserId
          version
        }
      }
    }
  }
}